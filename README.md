# Forkbin

### Mock now, use it anywhere

#### Getting started

1. Clone the repo
2. Install dependencies `pnpm install` or `npm install`
3. Clone .template.env and change env variables
4. Run docker daemon: `docker compose up -d
5. Run `pnpm prisma init` to execute database
6. Execute app `pnpm run dev`
